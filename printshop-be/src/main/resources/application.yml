spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/printshop?useUnicode=true&characterEncoding=utf8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate    # dev c� th? d�ng update
    properties:
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect
